aws ssm list-command-invocations \
  --filters "Key=document-name,Values=${something}" \
  --query "CommandInvocations[0].CommandId" \
  --output text

aws ssm wait command-executed --command-id "$COMMAND_ID" --instance-id ""

